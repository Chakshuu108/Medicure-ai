# Local `npm run dev` can leave this empty — Vite proxies /api to localhost:8000.
# On a static host (Render), set this at BUILD time to the public backend URL, no trailing slash:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
